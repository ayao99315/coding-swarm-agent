## 16:01 完成：SWARM-T001 on-complete 汇总通知修复
- 在 `skills/coding-swarm-agent/scripts/on-complete.sh` 新增 swarm 全部完成后的汇总通知，放在 token milestone 检查之后、单任务通知之前。
- 汇总通知会统计 project、done 任务数、唯一 commit 数、累计 input/output/cache tokens，并用 `/tmp/agent-swarm-complete/*.sent` 做一次性去重，避免最后几个任务并发结束时重复发消息。
- 复核了 `parse-tokens.sh -> update-task-status.sh -> swarm/active-tasks.json` 链路；确认 Codex/Claude token 已能落盘，`update-task-status.sh` 本次无需改动。
- 验证：`bash -n` 通过；`parse-tokens.sh` 对真实 Codex/Claude 日志均能解析；按要求执行了 `active-tasks.json` token 检查命令。
- 提交并推送：`61fba08 feat(on-complete): add swarm-complete summary notification with token totals`
- 遗留问题：无

## 16:33 完成：SWARM-T002 dispatch 链路简化（Plan B）
- 修改 `skills/coding-swarm-agent/scripts/on-complete.sh`：移除 `/hooks/agent` webhook hop，改为在同步更新 `active-tasks.json` 后直接 `openclaw system event` 唤醒主 session。
- 保留并验证 `skills/coding-swarm-agent/scripts/update-task-status.sh` 的 blocked→pending 自动解锁；同时清理了 `dispatch.sh` / `update-task-status.sh` 里的旧 webhook 注释。
- 验证：`bash -n` 通过；用临时 `HOME` + fake `openclaw` 跑了隔离测试，确认 `on-complete.sh` 会把 `T1` 标为 done、把依赖任务 `T2` 从 blocked 翻到 pending、把 agent 置 idle，并发出主 session system event。
- 遗留问题：未更新 `swarm/active-tasks.json` 里的 SWARM-T002 状态，也未提交 git commit；当前工作区已有其他脏改动，保持原样。

## 16:38 完成：SWARM-T002 提交收口
- 将 `skills/coding-swarm-agent/scripts/on-complete.sh` 的主 session 通知进一步收敛为 `openclaw system event --text "Done: $TASK_ID $TASK_NAME" --mode now`，并从 `active-tasks.json` 解析任务名。
- `skills/coding-swarm-agent/scripts/update-task-status.sh` 保留同步 blocked→pending 自动解锁；`bash -n` 已再次验证 `on-complete.sh` 与 `update-task-status.sh` 语法通过。
- 提交：`e2abe56 refactor(swarm): simplify dispatch - remove webhook, add auto-unblock in update-task-status`
- 已同步状态：`SWARM-T002` 标记为 done，`codex-1` 标记为 idle，`SWARM-T003` 现为 pending。
- 遗留问题：无

## 17:20 完成：SWARM-REVIEW2 审查 T002+T003+T004
- 审查了 `bcc6d54`、`e2abe56`、`61fba08`，并运行了 `git diff HEAD~3 HEAD -- skills/coding-swarm-agent/ | head -200`。
- 结论：`update-task-status.sh` 的 blocked→pending 自动解锁逻辑可通过；但 `dispatch.sh --prompt-file` 仍有关键问题，`on-complete.sh` 的 swarm-complete 汇总范围也不对。
- 主要问题：
- `dispatch.sh` 现在把命令当作 argv 解析，但 `SKILL.md` 仍按单个带引号字符串示例调用；按文档调用会生成 `COMMAND=( codex\ exec\ ... )`，最终报 `command not found`。
- `dispatch.sh` 的 `mktemp \"/tmp/...XXXXXX.txt\"` 在 macOS 下不会替换 `XXXXXX`，临时文件并不真正唯一。
- `dispatch.sh` 对 Codex 仍是把 tempfile 内容重新读回 argv 末尾，不是直接把文件交给 `codex exec`；只解决了一部分 quoting 问题。
- `on-complete.sh` 判断“全部完成”时扫描整个 `swarm/active-tasks.json`；按当前文件内容，`SWARM-REVIEW2` 完成后会汇总 27 个历史任务、23 个 commit、715,045 input tokens，而不是只汇总当前 swarm。
- 遗留问题：需要后续修复 `dispatch.sh` 调用兼容性、macOS tempfile 创建方式，以及 swarm-complete 的任务范围判定。
