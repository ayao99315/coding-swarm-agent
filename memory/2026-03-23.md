## 20:30 完成：修复 swarm completion 通知批次耗时与 per-task 摘要
- 修改 `skills/coding-swarm-agent/scripts/on-complete.sh`
- 修正 swarm-complete 通知的批次耗时计算：多任务批次改为 `updated_at` 区间，单任务若 `created_at -> updated_at` 超过 6 小时则跳过耗时显示
- 新增 per-task 摘要块：按任务顺序展示 task id、状态、任务名、tokens、短耗时，以及首个 commit 的短 hash + subject；异常时仅记错误日志，不阻断通知
- 结果：`bash -n ~/.openclaw/workspace/skills/coding-swarm-agent/scripts/on-complete.sh` 通过；commit `ed8c7c2` 已推送到 `origin/main`
- 遗留问题：尚未跑真实 swarm 完成场景做端到端通知验收，需等待下一个批次触发
